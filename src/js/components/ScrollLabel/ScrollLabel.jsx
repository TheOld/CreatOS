import React, { Component } from 'react';

import './scrollLabel.scss';

class ScrollLabel extends Component {
  render() {
    return (
      <span styleName="label">
        SCROLL
      </span>
    );
  }
}

export default ScrollLabel;
